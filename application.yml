spring:
  application:
    name: tarkov-service
  management:
    endpoints:
      web:
        exposure:
          include:
            - '*'
tarkov-service:
  tarkov: "development"
  default-value: "EFT"